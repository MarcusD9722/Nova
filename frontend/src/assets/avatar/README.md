# Nova avatar asset

Place the production avatar at `nova.glb` in this directory. Vite discovers and bundles it automatically; the application uses the procedural hologram fallback while it is absent.

Recommended model orientation: Y-up, facing +Z, with the origin near the center of the bust. The renderer normalizes scale and recenters the model at runtime.
