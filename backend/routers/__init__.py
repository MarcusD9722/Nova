"""Domain routers extracted from backend/app.py (Phase 0.6 of docs/ROADMAP.md).

Each module owns one endpoint domain and exposes an APIRouter as `router`.
Shared state comes from backend/state.py — never import backend.app here.
"""
